import React from "react";

// the next thing to do is to fetch the workouts from the backend
//hold the workouts in state using useState

const Workouts = () => {
  return (
    <div>
      <p>Here's where the workout list will be!</p>
    </div>
  );
};

export default Workouts;
